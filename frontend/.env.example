# Copy this file to .env and point it at your backend.
# Local development (matches backend running on port 8000):
VITE_API_URL=http://localhost:8000

# Production (set this in Vercel's environment variables instead of committing it here):
# VITE_API_URL=https://your-inference-api.example.com
